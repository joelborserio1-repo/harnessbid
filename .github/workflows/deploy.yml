name: Deploy (manual)

# Manual-only: this never runs on push. Trigger from the Actions tab.
on:
  workflow_dispatch:
    inputs:
      ref:
        description: "Git ref to deploy"
        required: false
        default: ""

concurrency:
  group: deploy-production
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ inputs.ref || github.ref }}
          fetch-depth: 0

      - name: Configure SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.DEPLOY_SSH_KEY }}" > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key

      - name: Run deploy
        env:
          DEPLOY_SSH_KEY: ~/.ssh/deploy_key
          DEPLOY_SSH_HOST: ${{ secrets.DEPLOY_SSH_HOST }}
          DEPLOY_SSH_USER: ${{ secrets.DEPLOY_SSH_USER }}
          # Root deploy (no /v2 base path). Site path from the xCloud panel.
          DEPLOY_BASE_PATH: ""
          DEPLOY_WEB_DIR: /var/www/harnessbid.com
          DEPLOY_HEALTHCHECK_URL: https://harnessbid.com/
        run: |
          chmod +x deploy/deploy.sh
          ./deploy/deploy.sh
